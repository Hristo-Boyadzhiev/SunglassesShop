import { DeliveryCostPipe } from './delivery-cost.pipe';

describe('DeliveryCostPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryCostPipe();
    expect(pipe).toBeTruthy();
  });
});
